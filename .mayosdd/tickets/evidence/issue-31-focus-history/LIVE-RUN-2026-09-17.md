# Live run 2026-09-17 — issue #31 at `deaf518`

Build: `make install` from `v0.4/issue-31-focus-history` at `deaf518`; Helium; extension `0.4.0-trial` (manifest not yet bumped — L3 in the review ledger).

## Loop

`node scripts/focus-loop.mjs run --scenarios close/` → `verdict-2026-09-17T23-20-49-256Z.json`: `close/immediate`, `close/switch-then-refocus`, `close/unfocused-red-button` all green. Trace: `trace-2026-09-17T23-20-49-256Z.jsonl`.

What the trace shows, per focused close (4 of 6):

| event | offset from `tab-removed` |
| --- | --- |
| `tab-removed` `isWindowClosing:true heldFocus:true` | 0 ms |
| `focus-changed` → primary window (Chromium's key handoff) | +27–32 ms |
| `window-removed` `wasFocused:true` | +29–34 ms |
| `restore-attempt` `sent-to-host` | +30–35 ms |
| `focus-changed` → none (the restored app is frontmost) | +48–58 ms |

Unfocused closes (2 of 6): `isWindowClosing:true heldFocus:false`, no handoff, no restoration, z-order unchanged.

Probe z-order after a focused close: the restored app at 0, **the primary Helium window at 1**, the app that was second before the open at 2. The loop judges the frontmost app only, so this reads green. It is what Lucas saw as "brings the primary window in front of my other apps".

## Manual checks (host log `~/.lilchromium/host-helium.log`)

- `23:23:41Z`, `23:27:46Z` — link clicked in Mail: `open … appPriorContext=none`. The extension then recorded the focused normal window; the close returned to it. Root cause: LaunchServices activates Lil Chromium before the URL handler runs, so the frontmost app at send time was the app itself.
- `23:30:08Z`, `23:31:12Z` — the same URL sent by script with Mail frontmost: `appPriorContext=external-app:pid=86346,bundleId=com.apple.mail`, `restore-focus … outcome=activated-exact-pid`, `frontmost-after … com.apple.mail`.

Fixes: ledger entries L1–L3.
