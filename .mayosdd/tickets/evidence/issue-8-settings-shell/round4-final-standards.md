1. S1 — `SettingsWindow.swift` `NSWindow` `contentRect` 460×560 vs `SettingsRoot` `.frame(width: 440, height: 520)` / `placeNearCenteredPalette` — JUDGEMENT — possible Primitive Obsession (S3): PROTOCOL.md Placement (v4) needs one defined size so the 20% top edge is computed from that size; placement still passes `window.frame.size` from the 460×560 contentRect, not the SwiftUI 440×520 frame.
