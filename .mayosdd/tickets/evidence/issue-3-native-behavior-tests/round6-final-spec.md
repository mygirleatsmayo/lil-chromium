No spec findings.

Issue #3 acceptance criteria are met in `main...HEAD`: runnable Swift Testing target (`swift-tools-version:6.0`, `.swiftLanguageMode(.v5)`); repo-root `fixtures/` for v0.3 inputs and v0.4 meanings; additive defaults, unknown-field preservation, and normalized encoding; URL intent, search templates, routing order, and message encode/decode; no live HOME/sockets/browsers in the suite. Palette ranking / Return modifiers / prior-context remain out of scope per ledger P1 and P4. Open ledger rows are none; settled won't-fix and interpretations were not reopened.
