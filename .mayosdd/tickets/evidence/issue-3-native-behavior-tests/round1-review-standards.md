1. S1: `mac/Tests/LilChromiumTests/ConfigMergeTests.swift` (`testSaveReplacesOwnedNestedObjectsWhole`) — JUDGEMENT — Test comment treats nested whole-replace (`sleep.wakeFeedback` dropped) as the `docs/PROTOCOL.md` rewrite contract; PROTOCOL.md only says unknown fields are preserved on rewrite (`AGENTS.md`: contract lives in PROTOCOL.md), so the new test pins a narrower rule PROTOCOL.md does not state.
2. S2: `mac/Tests/LilChromiumTests/Fixtures/config-v2-complete.json` / `message-context.json` (`sleep`, `searchEngine`, `hoverBar`) — JUDGEMENT — Duplicated Code: the same nested config objects are copied in both fixtures, against this change’s own `Fixture.swift` claim that a contract change is expressed in one place.
3. S3: `mac/Tests/LilChromiumTests/ConfigTests.swift` (`testLegacyConfigGetsAdditiveDefaults` comment) — JUDGEMENT — Mysterious Name: “Lil Nap” is not a PROTOCOL.md / `LilConfig` term (those use sleep and hover bar); `lilNap` exists only as an unknown-field probe in `config-with-unknown-fields.json`.
