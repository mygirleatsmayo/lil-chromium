1. **S1** — `mac/Sources/LilShared/Config.swift:107-108` `name: String = SearchProviders.startpage.name` / `template: String = SearchProviders.startpageTemplate`; `:221` `sleep-off / Startpage`; `mac/Tests/LilChromiumTests/ConfigTests.swift:21-22` `searchEngine is Startpage (issue #10); every other missing field follows PROTOCOL.md`. Breaks AGENTS.md (“A contract change lands in all three components or none”) and PROTOCOL.md (`searchEngine` default Google; “Missing file/fields → built-in defaults above”; “Any change here must update all three”). Failure: missing `config.json` (or a v1 file with no section) makes the native palette search Startpage while PROTOCOL and extension `DEFAULT_SEARCH` stay Google, so palette and hoverbar disagree until the app writes a file.

2. **S2** — `mac/Sources/LilShared/Config.swift:100-103` `public var provider: String` / `case name, template, provider`; `mac/Sources/LilShared/SearchProviders.swift:59-60` `google, duckDuckGo, bing, kagi, startpage, custom`. Breaks PROTOCOL.md `searchEngine` schema (`name` + `template` only) and “Presets in Settings: Google, DuckDuckGo, Bing, Kagi, Custom.” Failure: every native save and every `get-context` (`ContextMessage.searchEngine` is this Codable type) writes `provider` and Settings offers Startpage; PROTOCOL and the extension were not updated, so the documented contract and the on-disk/wire shape diverge.
