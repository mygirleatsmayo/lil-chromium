# Integrated live runs — 2026-08-26

Environment for both runs: Helium `0.15.7.1`, Lil Chromium app/host `v0.2-205-gf38dd44`, extension UI `0.4.0-trial`, worktree commit `f38dd44`, live diagnostic seam confirmed.

## Full matrix

Verdict: `verdict-2026-08-26T22-01-38-768Z.json`

Accepted observations:

- `close/immediate`: red 2/2 — focus landed on Helium instead of Mail.
- `close/switch-then-refocus`: red 2/2 — focus landed on Helium instead of cmux.
- `close/unfocused-red-button`: green 2/2 — cmux remained foreground.
- Correctly positioned opening cases were green.

Do not use this run's red placement verdicts for `open/cross-display/no-other-lil` or `open/cross-display/lil-on-primary-display`: the cursor was left on display 0, and Lil Chromium correctly anchors new Lils to the cursor. The harness prompt failed to state that the cursor had to return to Mail's display.

## Corrective cross-display run

Verdict: `verdict-2026-08-26T22-39-22-253Z.json`

Lucas kept the cursor over Mail for the measured repetitions. Results:

- `open/cross-display/no-other-lil`: red 3/3 — one unrelated Helium window rose with the requested Lil.
- `open/cross-display/lil-on-source-display`: green 3/3 — only the requested Lil came forward.
- `open/cross-display/lil-on-primary-display`: red 3/3 — one unrelated Helium window rose with the requested Lil.

This corrective run reproduces the opening regression independently of the contaminated placement readings. Combined with the valid close observations above, the current integrated v0.4 trial reproduces both focus regressions named by issue #30.

## Trace handling

The committed traces are mechanically sanitized copies of the raw captures. Sanitization preserved every record, ordering identifier, diagnostic field, and replay verdict while replacing unrelated browsing URLs.

In the corrective trace, three duplicate log fields used the intentional test URL as `https://example.com` without a trailing slash. The literal sanitizer allowlist redacted those three values; normalized `https://example.com/` events remain in the same trace. JSON parsing, event counts, ordering, and replay verdicts are unchanged.
