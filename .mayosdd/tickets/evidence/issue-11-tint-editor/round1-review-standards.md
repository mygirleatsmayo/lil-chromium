1. **S1 (high)** — `mac/Sources/LilChromiumApp/TintValue.swift:102` `guard let hex else { return "" }`, written through `SettingsWindow.swift:472` `store.config.sleep.tint = TintValue.sleepStorage(fromCommitted: newHex)`, and locked by `TintTests.swift:131` / `:198` (`sleepStorage(fromCommitted: nil) == ""`, `decoded.sleep.tint == ""`). **Rule:** `AGENTS.md`: “A contract change lands in all three components or none.” `docs/PROTOCOL.md`: “Any change here must update all three”; `sleep.tint` is `"gray" | "purple"` or `#rrggbb` (schema default `"purple"`). This diff does not change `docs/PROTOCOL.md`, `extension/`, or the host. **Failure:** Lil Nap “No tint” persists `"tint":""`. That value is not in the contract. Unchanged `extension/background.js` treats a falsy tint as `"purple"` and omits the query param; `extension/sleep.js` `resolveTint` maps empty/unknown to purple. A napping lil stays purple, and the three components no longer share one `config.json` meaning.
