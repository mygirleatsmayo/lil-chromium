- P1 (a) "Include literal registrable-domain prefix and interior matches, subdomain-only matches, title/path matches, novel text, URL-like input, dense and scattered fuzzy input, duplicate hosts/paths, and all exact Return modifier combinations." — Palette tests cover prefix/URL/novel/dedupe only; they never put Search above subdomain/title/path/fuzzy-only hits and never test Return / Shift+Return / Command+Return action selection.
- P2 (c) "Representative JSON fixtures express v0.3 compatibility inputs and v0.4 contract meanings without duplicating equivalent fixtures per test suite." — `config-with-unknown-fields.json` invents top-level `lilNap.revealZonePx` / `restorePriorContext`; v0.4’s real shapes are nested `hoverBar.revealHeight` and per-lil prior-context payloads, so the merge tests miss the owned-object path those fields will use.
- P3 (a) "Use shared representative JSON fixtures at the app/host/extension boundary so config and message changes cannot land in only one component." — Fixtures live only as `mac/Tests/LilChromiumTests/Fixtures/` test-bundle copies; the host/extension cannot share them.
- P4 (a) "Exercise palette ranking and action selection, URL intent, search templates, additive config defaults/unknown-field preservation, browser-installation routing, prior-context payloads, and message encoding/decoding." — No prior-context message fixture or encode/decode test exists.
