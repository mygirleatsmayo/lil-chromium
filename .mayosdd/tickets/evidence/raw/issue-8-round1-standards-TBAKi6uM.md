1. S1 — `mac/Sources/LilChromiumApp/MainMenu.swift` `windowMenu()` (Close ⌘W / Minimize ⌘M) — HARD — `docs/PROTOCOL.md` App behavior contract: palette dismisses ONLY on Esc, ⌘⌥N, X, opening a result, or showing Settings. This hunk adds nil-targeted `performClose:` / `performMiniaturize:` with those keys. The same file’s `verified:` comment says `NSApp.sendEvent` fires main-menu key equivalents while the `.nonactivatingPanel` is key, so ⌘W/⌘M hit the palette. That is a new dismiss/miniaturize path; `performClose` is not `PaletteController.close()` (`orderOut`).
2. S2 — `mac/Sources/LilChromiumApp/PalettePlacement.swift` `enum PalettePlacement` / `activeScreen` — JUDGEMENT — `AGENTS.md`: annotate AppKit-touching classes `@MainActor` (Swift 6.2 isolation hard errors). New AppKit-touching type (`NSEvent.mouseLocation`, `NSScreen`, `NSMouseInRect`) has no `@MainActor`, unlike `MainMenu` (also an enum) in this diff. `activeScreen` asserts menu-bar `NSScreen.main` behavior without a `verified:` marker (`AGENTS.md`).
3. S3 — `PalettePlacement.centeredOrigin(forSize:on:)` used by `SettingsWindow.swift` `placeNearCenteredPalette` — JUDGEMENT — `docs/PROTOCOL.md` Placement (v4): first presentation top edge at 20% of `visibleFrame` height. The helper does `max(y, vf.minY)`, so a tall Settings window is nudged up and the top edge is no longer at 20%.
